version: 1
repos:
  - slug: Prekzursil/pbinfo-get-unsolved
    profile: pbinfo-get-unsolved
    rollout: fixture
    default_branch: main
    notes: Dry-run fixture and portability source for strict-zero scripts.
  - slug: Prekzursil/quality-zero-platform
    profile: quality-zero-platform
    rollout: phase2-wave0
    default_branch: main
    notes: Self-governed control-plane repo with wrapper parity and provider baseline checks.
  - slug: Prekzursil/codex-session-manager
    profile: codex-session-manager
    rollout: phase1-wave7
    default_branch: main
    notes: Windows WPF desktop repo pending repo-side strict-zero wrapper and coverage-script adoption.
  - slug: Prekzursil/Airline-Reservations-System
    profile: airline-reservations-system
    rollout: phase1-wave4
    default_branch: main
    notes: Airline overlay with native DeepSource contexts.
  - slug: Prekzursil/DevExtreme-Filter-Go-Language
    profile: devextreme-filter-go-language
    rollout: phase1-wave2
    default_branch: main
    notes: Common template cohort.
  - slug: Prekzursil/event-link
    profile: event-link
    rollout: phase2-wave1
    default_branch: main
    notes: Fullstack overlay with backend-plus-ui verify wrapper and quality-only gate set.
  - slug: Prekzursil/Personal-Finance-Management
    profile: personal-finance-management
    rollout: phase1-wave2
    default_branch: main
    notes: Common template cohort.
  - slug: Prekzursil/momentstudio
    profile: momentstudio
    rollout: phase2-wave2
    default_branch: main
    notes: Web visual repo with Chromatic plus Applitools pairing and Percy retirement.
  - slug: Prekzursil/Reframe
    profile: reframe
    rollout: phase1-wave5
    default_branch: main
    notes: Broadest custom context set; migrate last.
  - slug: Prekzursil/SWFOC-Mod-Menu
    profile: swfoc-mod-menu
    rollout: phase1-wave3
    default_branch: main
    notes: Preserve PR-head-SHA handling and legacy policy metadata.
  - slug: Prekzursil/Star-Wars-Galactic-Battlegrounds-Save-Game-Editor
    profile: star-wars-galactic-battlegrounds-save-game-editor
    rollout: phase1-wave2
    default_branch: main
    notes: Common template cohort.
  - slug: Prekzursil/TanksFlashMobile
    profile: tanksflashmobile
    rollout: phase1-wave2
    default_branch: main
    notes: Common template cohort.
  - slug: Prekzursil/WebCoder
    profile: webcoder
    rollout: phase1-wave2
    default_branch: main
    notes: Common template cohort.
  - slug: Prekzursil/env-inspector
    profile: env-inspector
    rollout: phase1-wave3
    default_branch: main
    notes: Preserve PR-focused trigger behavior metadata.
  - slug: Prekzursil/codeblocks-pretty-prints-stable
    profile: codeblocks-pretty-prints-stable
    rollout: phase1-wave6
    default_branch: main
    notes: Public fetch-and-package Code::Blocks control-plane repo with managed profile seeding and strict-zero governance.
  - slug: Prekzursil/PrimariRo
    profile: primariro
    rollout: observe
    default_branch: main
    notes: Lean member (private repo). Ruleset models the repo's own aggregate CI gate (Required / all CI and privacy gates) plus the platform CodeQL baseline context; no active ruleset on main today; coverage informational, not CI-enforced.
  - slug: Prekzursil/tracelines
    profile: tracelines
    rollout: observe
    default_branch: main
    notes: Lean member (public repo). Ruleset models the repo's own ci.yml checks (mypy + 6-leg test matrix) plus the platform CodeQL baseline context; matrix-derived context names are brittle across Python-version changes; coverage measured but informational; CodeQL runs today via GitHub default setup, which does not emit the managed workflow context.
  - slug: Prekzursil/ai-sessions-render
    profile: ai-sessions-render
    rollout: observe
    default_branch: main
    notes: Lean member (public repo, dual-language Python + JS/TS). Ruleset models the repo's own ci.yml (Python 4-leg matrix + JS 2-leg matrix + two packaged-artifact smoke jobs) plus the advanced CodeQL baseline (python + javascript-typescript analyze jobs). UNLIKE tracelines, coverage is ENFORCED in-repo (pytest --cov-fail-under=100 statements+branches; vitest 100% stmt/line/func), so a regression reddens the repo's own test contexts; QZP collects no coverage artifact. Dependabot + Private Vulnerability Reporting enabled at the repo. No active ruleset on main today (observe).
  - slug: Prekzursil/agent-skills-toolchain
    profile: agent-skills-toolchain
    rollout: observe
    default_branch: main
    notes: Lean member. The 7 gate-* quality.yml job contexts are already enforced by active repo ruleset 18297434 on main; the platform CodeQL baseline context is added on top; coverage is per-package 100 in repo CI, whole-repo aggregate deliberately not modeled.
